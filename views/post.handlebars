<div class="text-center">
    <h2>{{title}}</h2>
    <p>{{content}}</p>
    <p>Created by {{User.first_name}} {{User.last_name}} on {{format_date created_at}}</p>
    <p>
        <button class="btn btn-primary">Comment on this post!</button>
    </p>
</div>